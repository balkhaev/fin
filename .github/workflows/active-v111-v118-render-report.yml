name: Render Active V111-V118 report

on:
  push:
    branches: [agent/active-v111-v118-trident]
    paths:
      - "research/active_v111_v118/render_report.py"
      - ".github/workflows/active-v111-v118-render-report.yml"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  render:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          ref: agent/active-v111-v118-trident
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - run: python -m pip install 'pandas>=2.2,<3'
      - name: Render report from frozen summary
        run: |
          set -euxo pipefail
          test -s research/active_v111_v118/results/summary.json
          test -s research/active_v111_v118/results/v117_yearly.csv
          python research/active_v111_v118/render_report.py
          test -s research/active_v111_v118/REPORT_RU.md
      - name: Commit report
        run: |
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add research/active_v111_v118/REPORT_RU.md
          git commit -m "Document Active V111-V118 TRIDENT result [skip ci]"
          git push origin HEAD:agent/active-v111-v118-trident
