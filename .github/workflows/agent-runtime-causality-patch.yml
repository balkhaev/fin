name: Apply runtime causality patch

on:
  push:
    branches: [agent/runtime-causality-progress-20260730]
    paths:
      - '.github/workflows/agent-runtime-causality-patch.yml'

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/runtime-causality-progress-20260730
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Apply reviewed runtime patch
        run: |
          set -euxo pipefail
          cat >/tmp/runtime-causality.patch <<'PATCH'
          diff --git a/src/finruntime/models.py b/src/finruntime/models.py
          index 8e889c2..0000000 100644
          --- a/src/finruntime/models.py
          +++ b/src/finruntime/models.py
          @@ -62,12 +62,20 @@ class SourceObservation:
               def validate(self, *, decision_time_utc: str | None = None) -> None:
                   if not self.source:
                       raise ContractError("source cannot be empty")
          -        parse_utc(self.source_timestamp_utc)
          +        source_time = parse_utc(self.source_timestamp_utc)
                   available = parse_utc(self.available_at_utc)
                   require_sha256(self.payload_sha256, field="payload_sha256")
                   if self.quality not in SOURCE_QUALITIES:
                       raise ContractError(f"unsupported source quality: {self.quality}")
          -        if decision_time_utc is not None and available > parse_utc(decision_time_utc):
          +        if source_time > available:
          +            raise ContractError(
          +                f"source {self.source!r} timestamp is later than availability"
          +            )
          +        if decision_time_utc is None:
          +            return
          +        decision = parse_utc(decision_time_utc)
          +        if source_time > decision:
          +            raise ContractError(f"source {self.source!r} is dated after decision time")
          +        if available > decision:
                       raise ContractError(
                           f"source {self.source!r} was not available at decision time"
                       )
          diff --git a/src/finruntime/portfolio/accounting.py b/src/finruntime/portfolio/accounting.py
          index 65074c7..0000000 100644
          --- a/src/finruntime/portfolio/accounting.py
          +++ b/src/finruntime/portfolio/accounting.py
          @@ -2,7 +2,7 @@ from __future__ import annotations
          
          -from dataclasses import asdict, dataclass, replace
          +from dataclasses import asdict, dataclass, field, replace
           from decimal import Decimal, localcontext
           from typing import Any, Mapping, Sequence
          @@ -49,6 +49,24 @@ def _decimal_mapping(
               return output
          
          
          +def _intent_fill_mapping(
          +    value: Mapping[str, str] | None,
          +) -> dict[str, Decimal]:
          +    output: dict[str, Decimal] = {}
          +    for intent_id, raw in (value or {}).items():
          +        normalized_id = require_sha256(
          +            str(intent_id), field="active_plan_intent_id"
          +        )
          +        output[normalized_id] = require_decimal_string(
          +            raw,
          +            field=f"active_plan_filled_quantities.{normalized_id}",
          +            minimum=Decimal("0.000000000001"),
          +        )
          +    return output
          +
          +
           def _text_mapping(value: Mapping[str, Decimal]) -> dict[str, str]:
               return {
                   instrument: decimal_text(number)
          @@ -131,6 +149,8 @@ class PaperAccountState:
               last_plan_id: str | None
               applied_event_ids: Sequence[str]
               account_hash: str
          +    active_plan_filled_quantities: Mapping[str, str] = field(default_factory=dict)
          +    active_plan_fill_event_ids: Sequence[str] = field(default_factory=tuple)
          
               @classmethod
               def create(
          @@ -150,9 +170,11 @@ class PaperAccountState:
                   high_water: str,
                   last_plan_id: str | None = None,
                   applied_event_ids: Sequence[str] = (),
          +        active_plan_filled_quantities: Mapping[str, str] | None = None,
          +        active_plan_fill_event_ids: Sequence[str] = (),
               ) -> "PaperAccountState":
                   provisional = cls(
          -            schema_version="1.0",
          +            schema_version="1.1",
                       strategy_id=strategy_id,
                       sequence=int(sequence),
                       as_of_utc=format_utc(as_of_utc),
          @@ -168,6 +190,10 @@ class PaperAccountState:
                       last_plan_id=last_plan_id,
                       applied_event_ids=tuple(applied_event_ids),
                       account_hash="sha256:" + "0" * 64,
          +            active_plan_filled_quantities=dict(
          +                active_plan_filled_quantities or {}
          +            ),
          +            active_plan_fill_event_ids=tuple(active_plan_fill_event_ids),
                   )
                   result = replace(
                       provisional,
          @@ -199,7 +225,7 @@ class PaperAccountState:
                   )
          
               def validate(self) -> None:
          -        if self.schema_version != "1.0" or not self.strategy_id:
          +        if self.schema_version not in {"1.0", "1.1"} or not self.strategy_id:
                       raise ContractError("invalid PaperAccountState identity")
                   if self.sequence < 0:
                       raise ContractError("paper account sequence must be non-negative")
          @@ -236,20 +262,45 @@ class PaperAccountState:
                   if any(value <= 0 for value in entries.values()):
                       raise ContractError("perpetual entry prices must be positive")
                   if self.last_plan_id is not None:
                       require_sha256(self.last_plan_id, field="last_plan_id")
          +        plan_fills = _intent_fill_mapping(self.active_plan_filled_quantities)
                   if len(set(self.applied_event_ids)) != len(self.applied_event_ids):
                       raise ContractError("applied event ids must be unique")
                   for event_id in self.applied_event_ids:
                       require_sha256(event_id, field="applied_event_id")
          +        if len(set(self.active_plan_fill_event_ids)) != len(
          +            self.active_plan_fill_event_ids
          +        ):
          +            raise ContractError("active plan fill event ids must be unique")
          +        for event_id in self.active_plan_fill_event_ids:
          +            require_sha256(event_id, field="active_plan_fill_event_id")
          +        if not set(self.active_plan_fill_event_ids).issubset(
          +            set(self.applied_event_ids)
          +        ):
          +            raise ContractError("active plan fill events must be applied to the account")
          +        if self.last_plan_id is None and (
          +            plan_fills or self.active_plan_fill_event_ids
          +        ):
          +            raise ContractError("active plan progress requires last_plan_id")
          +        if self.schema_version == "1.0" and (
          +            plan_fills or self.active_plan_fill_event_ids
          +        ):
          +            raise ContractError("legacy account state cannot contain plan progress")
                   require_sha256(self.account_hash, field="account_hash")
          -        expected = sha256_id(_hash_payload(self, {"account_hash"}))
          +        hash_payload = _hash_payload(self, {"account_hash"})
          +        if self.schema_version == "1.0":
          +            hash_payload.pop("active_plan_filled_quantities", None)
          +            hash_payload.pop("active_plan_fill_event_ids", None)
          +        expected = sha256_id(hash_payload)
                   if self.account_hash != expected:
                       raise ContractError("PaperAccountState hash mismatch")
                   # Force validation of normalized mappings even when their return values are unused.
          -        _ = spot, perp, entries
          +        _ = spot, perp, entries, plan_fills
          
               def to_dict(self) -> dict[str, Any]:
          -        return asdict(self)
          +        payload = asdict(self)
          +        if self.schema_version == "1.0":
          +            payload.pop("active_plan_filled_quantities", None)
          +            payload.pop("active_plan_fill_event_ids", None)
          +        return payload
          @@ -321,12 +372,25 @@ def _rebuild(
               high_water: Decimal,
               last_plan_id: str | None,
               applied_event_ids: Sequence[str],
          +    active_plan_filled_quantities: Mapping[str, Decimal] | None = None,
          +    active_plan_fill_event_ids: Sequence[str] | None = None,
               sequence_increment: int = 1,
           ) -> PaperAccountState:
               if cash < 0:
                   raise AccountingHalt("paper cash cannot become negative")
               if equity <= 0:
                   raise AccountingHalt("paper equity cannot become non-positive")
          +    normalized_as_of = format_utc(as_of_utc)
          +    if parse_utc(normalized_as_of) < parse_utc(state.as_of_utc):
          +        raise AccountingHalt("paper account time cannot move backward")
          +    plan_fills = (
          +        dict(active_plan_filled_quantities)
          +        if active_plan_filled_quantities is not None
          +        else _intent_fill_mapping(state.active_plan_filled_quantities)
          +    )
          +    plan_events = (
          +        tuple(active_plan_fill_event_ids)
          +        if active_plan_fill_event_ids is not None
          +        else tuple(state.active_plan_fill_event_ids)
          +    )
               return PaperAccountState.create(
                   strategy_id=state.strategy_id,
                   sequence=state.sequence + sequence_increment,
          -        as_of_utc=as_of_utc,
          +        as_of_utc=normalized_as_of,
                   cash=decimal_text(cash),
                   spot_positions=_text_mapping(spot),
                   perp_positions=_text_mapping(perp),
          @@ -342,6 +406,8 @@ def _rebuild(
                   high_water=decimal_text(max(high_water, equity)),
                   last_plan_id=last_plan_id,
                   applied_event_ids=tuple(applied_event_ids),
          +        active_plan_filled_quantities=_text_mapping(plan_fills),
          +        active_plan_fill_event_ids=plan_events,
               )
          @@ -394,6 +460,10 @@ def apply_fill_event(
               if state.last_plan_id is not None and state.last_plan_id != fill.plan_id:
                   raise AccountingHalt("fill belongs to a different plan")
          
               cash, spot, perp, entries, fees, realized, funding, equity, high_water = _state_values(state)
               applied = tuple(state.applied_event_ids) + (fill.event_id,)
          +    plan_fills = _intent_fill_mapping(state.active_plan_filled_quantities)
          +    plan_events = tuple(state.active_plan_fill_event_ids) + (fill.event_id,)
               if fill.status in {"rejected", "expired"}:
                   return _rebuild(
                       state,
          @@ -411,6 +481,8 @@ def apply_fill_event(
                       high_water=high_water,
                       last_plan_id=fill.plan_id,
                       applied_event_ids=applied,
          +            active_plan_filled_quantities=plan_fills,
          +            active_plan_fill_event_ids=plan_events,
                   )
          
               quantity = require_decimal_string(
          @@ -424,8 +496,17 @@ def apply_fill_event(
                   field="intent.quantity",
                   minimum=Decimal("0.000000000001"),
               )
          -    if quantity > requested:
          -        raise AccountingHalt("filled quantity exceeds intent quantity")
          +    cumulative = plan_fills.get(intent.intent_id, _ZERO) + quantity
          +    if cumulative > requested:
          +        raise AccountingHalt("cumulative filled quantity exceeds intent quantity")
          +    if fill.status == "filled" and cumulative != requested:
          +        raise AccountingHalt("filled status requires cumulative intent completion")
          +    if fill.status == "partial" and cumulative >= requested:
          +        raise AccountingHalt("partial status cannot complete an intent")
          +    plan_fills[intent.intent_id] = cumulative
               price = require_decimal_string(
                   fill.price,
                   field="fill.price",
          @@ -495,6 +576,8 @@ def apply_fill_event(
                   high_water=high_water,
                   last_plan_id=fill.plan_id,
                   applied_event_ids=applied,
          +        active_plan_filled_quantities=plan_fills,
          +        active_plan_fill_event_ids=plan_events,
               )
          diff --git a/src/finruntime/portfolio/lifecycle.py b/src/finruntime/portfolio/lifecycle.py
          index e1451b3..0000000 100644
          --- a/src/finruntime/portfolio/lifecycle.py
          +++ b/src/finruntime/portfolio/lifecycle.py
          @@ -2,8 +2,8 @@ from __future__ import annotations
          
          -from finruntime.canonical import require_sha256
          -from finruntime.portfolio.accounting import PaperAccountState
          +from finruntime.canonical import parse_utc, require_sha256
          +from finruntime.portfolio.accounting import AccountingHalt, PaperAccountState
          @@ -15,6 +15,8 @@ def activate_paper_plan(
               """Open a new immutable paper-plan context before applying its fill events."""
          
               state.validate()
          +    if parse_utc(as_of_utc) < parse_utc(state.as_of_utc):
          +        raise AccountingHalt("paper plan activation cannot move account time backward")
               plan_id = require_sha256(plan_id, field="plan_id")
               if state.last_plan_id == plan_id:
                   return state
          diff --git a/src/finruntime/execution/paper_cycle.py b/src/finruntime/execution/paper_cycle.py
          index 90d65dc..0000000 100644
          --- a/src/finruntime/execution/paper_cycle.py
          +++ b/src/finruntime/execution/paper_cycle.py
          @@ -25,18 +25,17 @@ def execute_paper_cycle(
               policy: PaperBrokerPolicy = DEFAULT_PAPER_BROKER_POLICY,
           ) -> PaperExecutionResult:
          -    """Activate and execute one immutable paper plan exactly once.
          +    """Activate or resume an immutable paper plan against sealed observations.
          
          -    A partially or fully executed plan must be replanned from the resulting account state.
          -    Reusing the same plan against later quotes could otherwise overfill its original intents.
          +    Schema 1.1 account state carries cumulative per-intent fills, so later observations
          +    can safely complete a partial plan without exceeding the original quantities.
               """
          
               plan.validate()
               account_state.validate()
          -    if account_state.last_plan_id == plan.plan_id:
          +    if account_state.last_plan_id == plan.plan_id and account_state.schema_version == "1.0":
                   raise AccountingHalt(
          -            "paper plan was already activated; build a new plan from current account state"
          +            "legacy account state lacks resumable plan progress; build a new plan"
                   )
          
               active = activate_paper_plan(
          diff --git a/src/finruntime/execution/paper_broker.py b/src/finruntime/execution/paper_broker.py
          index 1130d5b..0000000 100644
          --- a/src/finruntime/execution/paper_broker.py
          +++ b/src/finruntime/execution/paper_broker.py
          @@ -249,18 +249,62 @@ def _current_reduce_capacity(
               return abs(current)
          
          
          +def _intent_quantity(intent: ExecutionIntent) -> Decimal:
          +    return require_decimal_string(
          +        intent.quantity,
          +        field=f"intent.quantity.{intent.intent_id}",
          +        minimum=Decimal("0.000000000001"),
          +    )
          +
          +
          +def _plan_progress(
          +    plan: ExecutionPlan,
          +    account: PaperAccountState,
          +) -> tuple[dict[str, ExecutionIntent], dict[str, Decimal]]:
          +    if account.last_plan_id != plan.plan_id:
          +        raise AccountingHalt("paper account is not activated for this plan")
          +    if account.schema_version == "1.0":
          +        raise AccountingHalt("legacy account state has no active plan progress")
          +    intents: dict[str, ExecutionIntent] = {}
          +    for intent in plan.intents:
          +        if intent.intent_id in intents:
          +            raise AccountingHalt("execution plan contains duplicate intent ids")
          +        intents[intent.intent_id] = intent
          +    progress: dict[str, Decimal] = {}
          +    for intent_id, raw in account.active_plan_filled_quantities.items():
          +        intent = intents.get(intent_id)
          +        if intent is None:
          +            raise AccountingHalt("account progress references an intent outside the plan")
          +        quantity = require_decimal_string(
          +            raw,
          +            field=f"active_plan_filled_quantities.{intent_id}",
          +            minimum=Decimal("0.000000000001"),
          +        )
          +        if quantity > _intent_quantity(intent):
          +            raise AccountingHalt("account progress exceeds an intent quantity")
          +        progress[intent_id] = quantity
          +    return intents, progress
          +
          +
          +def _plan_complete(
          +    intents: Mapping[str, ExecutionIntent],
          +    progress: Mapping[str, Decimal],
          +) -> bool:
          +    return all(progress.get(intent_id, _ZERO) == _intent_quantity(intent) for intent_id, intent in intents.items())
          +
          +
           def _make_outcome(
               intent: ExecutionIntent,
               fill: FillEvent,
               reason: str,
          +    requested_quantity: Decimal,
           ) -> PaperFillOutcome:
               return PaperFillOutcome(
                   intent_id=intent.intent_id,
                   event_id=fill.event_id,
                   status=fill.status,
                   reason=reason,
          -        requested_quantity=intent.quantity,
          +        requested_quantity=decimal_text(requested_quantity),
                   filled_quantity=fill.filled_quantity,
               )
          @@ -279,16 +323,27 @@ def execute_paper_plan(
               if plan.strategy_id != account_state.strategy_id:
                   raise AccountingHalt("paper plan and account strategy_id mismatch")
          
          +    intents_by_id, progress = _plan_progress(plan, account_state)
          +    if _plan_complete(intents_by_id, progress):
          +        return PaperExecutionResult(
          +            account_state=account_state,
          +            fill_events=(),
          +            outcomes=(),
          +            total_filled_notional="0",
          +            total_fees="0",
          +            weighted_slippage_bps="0",
          +            execution_complete=True,
          +        )
          +
               quote_by_key = _quote_map(quotes)
               state = account_state
               fills: list[FillEvent] = []
               outcomes: list[PaperFillOutcome] = []
          -    status_by_intent: dict[str, str] = {}
               total_notional = _ZERO
               total_fees = _ZERO
               slippage_notional = _ZERO
          -    latest_time = parse_utc(plan.created_at_utc)
          +    latest_time = max(parse_utc(plan.created_at_utc), parse_utc(state.as_of_utc))
          
               for intent in plan.intents:
                   intent.validate()
          +        total_requested = _intent_quantity(intent)
          +        already_filled = progress.get(intent.intent_id, _ZERO)
          +        requested = total_requested - already_filled
          +        if requested == 0:
          +            continue
                   quote = quote_by_key.get(_quote_key(intent))
                   source_hash = (
                       quote.source_observation_hash if quote is not None else plan.market_snapshot_id
                   )
          -        event_time = quote.observed_at_utc if quote is not None else plan.created_at_utc
          +        event_time = quote.observed_at_utc if quote is not None else state.as_of_utc
                   latest_time = max(latest_time, parse_utc(event_time))
          
          -        if intent.parent_intent_id is not None and status_by_intent.get(
          -            intent.parent_intent_id
          -        ) != "filled":
          +        parent = (
          +            intents_by_id.get(intent.parent_intent_id)
          +            if intent.parent_intent_id is not None
          +            else None
          +        )
          +        if intent.parent_intent_id is not None and parent is None:
          +            raise AccountingHalt("execution intent parent is outside the plan")
          +        if parent is not None and progress.get(parent.intent_id, _ZERO) != _intent_quantity(parent):
                       fill = _rejection_fill(
                           plan=plan,
                           intent=intent,
          @@ -349,11 +404,6 @@ def execute_paper_plan(
                       )
                       reason = f"quote_quality_{quote.quality}"
                   else:
          -            requested = require_decimal_string(
          -                intent.quantity,
          -                field="intent.quantity",
          -                minimum=Decimal("0.000000000001"),
          -            )
                       available = require_decimal_string(
                           quote.available_quantity,
                           field="quote.available_quantity",
          @@ -414,9 +464,12 @@ def execute_paper_plan(
          
                   state = apply_fill_event(state, intent, fill)
                   fills.append(fill)
          -        outcomes.append(_make_outcome(intent, fill, reason))
          -        status_by_intent[intent.intent_id] = fill.status
          +        outcomes.append(_make_outcome(intent, fill, reason, requested))
          +        if fill.status in {"partial", "filled"}:
          +            progress[intent.intent_id] = already_filled + require_decimal_string(
          +                fill.filled_quantity, field="fill.filled_quantity", minimum=_ZERO
          +            )
          
               marked = mark_account(
                   state,
          @@ -425,6 +478,9 @@ def execute_paper_plan(
               weighted_slippage = (
                   slippage_notional / total_notional if total_notional > 0 else _ZERO
               )
          +    final_progress = {
          +        key: require_decimal_string(value, field=f"active_plan_filled_quantities.{key}") for key, value in marked.active_plan_filled_quantities.items()
          +    }
               return PaperExecutionResult(
                   account_state=marked,
                   fill_events=tuple(fills),
          @@ -433,5 +489,5 @@ def execute_paper_plan(
                   total_filled_notional=decimal_text(total_notional),
                   total_fees=decimal_text(total_fees),
                   weighted_slippage_bps=decimal_text(weighted_slippage),
          -        execution_complete=all(fill.status == "filled" for fill in fills),
          +        execution_complete=_plan_complete(intents_by_id, final_progress),
               )
          diff --git a/src/finruntime/journal/atomic.py b/src/finruntime/journal/atomic.py
          index 270dab7..0000000 100644
          --- a/src/finruntime/journal/atomic.py
          +++ b/src/finruntime/journal/atomic.py
          @@ -12,6 +12,7 @@ from finruntime.canonical import (
               ContractError,
               canonical_json_bytes,
               format_utc,
          +    parse_utc,
               require_sha256,
               sha256_id,
           )
          @@ -32,6 +33,29 @@ _SINGLETON_EVENT_TYPES = {
               "HALT_CLEARED",
           }
          
          +_RUNTIME_EVENT_PHASES = {
          +    "SNAPSHOT_ACCEPTED": 10,
          +    "TARGET_COMPUTED": 20,
          +    "PLAN_CREATED": 30,
          +    "FILL_RECORDED": 40,
          +    "STATE_COMMITTED": 50,
          +    "RECONCILIATION_COMPLETED": 60,
          +    "HALT_RAISED": 70,
          +}
          +
          +_RUNTIME_EVENT_PREDECESSORS = {
          +    "TARGET_COMPUTED": {"SNAPSHOT_ACCEPTED"},
          +    "PLAN_CREATED": {"TARGET_COMPUTED"},
          +    "FILL_RECORDED": {"PLAN_CREATED"},
          +    "STATE_COMMITTED": {"PLAN_CREATED"},
          +    "RECONCILIATION_COMPLETED": {"STATE_COMMITTED"},
          +    "HALT_RAISED": {"RECONCILIATION_COMPLETED"},
          +}
          +
          
           def _write_bytes_atomic(path: Path, payload: bytes) -> None:
          @@ -124,6 +148,64 @@ class AppendOnlyJournal:
                   events.append(value)
               return events
          
          +    @staticmethod
          +    def _verify_semantics(events: Sequence[Mapping[str, Any]]) -> None:
          +        last_sequence: dict[str, int] = {}
          +        last_time: dict[str, object] = {}
          +        phase_by_cycle: dict[tuple[str, int], int] = {}
          +        seen_by_cycle: dict[tuple[str, int], set[str]] = {}
          +        halted: set[str] = set()
          +        for line_number, event in enumerate(events, 1):
          +            strategy_id = str(event["strategy_id"])
          +            sequence = int(event["sequence"])
          +            event_type = str(event["event_type"])
          +            event_time = parse_utc(str(event["event_time_utc"]))
          +            previous_sequence = last_sequence.get(strategy_id)
          +            if previous_sequence is not None and sequence < previous_sequence:
          +                raise JournalCorruptionError(
          +                    f"journal sequence moved backward on line {line_number}"
          +                )
          +            previous_time = last_time.get(strategy_id)
          +            if previous_time is not None and event_time < previous_time:
          +                raise JournalCorruptionError(
          +                    f"journal event time moved backward on line {line_number}"
          +                )
          +            last_sequence[strategy_id] = sequence
          +            last_time[strategy_id] = event_time
          +
          +            cycle_key = (strategy_id, sequence)
          +            seen = seen_by_cycle.setdefault(cycle_key, set())
          +            phase = _RUNTIME_EVENT_PHASES.get(event_type)
          +            if phase is not None:
          +                prior_phase = phase_by_cycle.get(cycle_key, -1)
          +                if phase < prior_phase:
          +                    raise JournalCorruptionError(
          +                        f"runtime event phase moved backward on line {line_number}"
          +                    )
          +                required = _RUNTIME_EVENT_PREDECESSORS.get(event_type, set())
          +                missing = required - seen
          +                if missing:
          +                    raise JournalCorruptionError(
          +                        f"runtime event {event_type} lacks predecessors {sorted(missing)} "
          +                        f"on line {line_number}"
          +                    )
          +                phase_by_cycle[cycle_key] = phase
          +                seen.add(event_type)
          +
          +            if event_type == "HALT_RAISED":
          +                halted.add(strategy_id)
          +            elif event_type == "HALT_CLEARED":
          +                if strategy_id not in halted:
          +                    raise JournalCorruptionError(
          +                        f"HALT_CLEARED without HALT_RAISED on line {line_number}"
          +                    )
          +                halted.remove(strategy_id)
          +
               @staticmethod
               def _verify_events(events: Iterable[Mapping[str, Any]]) -> list[dict[str, Any]]:
                   previous: str | None = None
          @@ -191,6 +273,7 @@ class AppendOnlyJournal:
                       singleton_keys[key] = str(event["payload_hash"])
                   seen.add(str(event["event_hash"]))
                   previous = str(event["event_hash"])
          +        AppendOnlyJournal._verify_semantics(normalized)
                   return normalized
          @@ -313,6 +396,7 @@ class AppendOnlyJournal:
                       by_identity[identity] = event
                       selected.append(event)
          
                   if new_events:
          +            self._verify_events([*existing, *new_events])
                       payload = b"".join(
                           canonical_json_bytes(event) + b"\n" for event in new_events
                       )
          diff --git a/schemas/runtime/market_snapshot.schema.json b/schemas/runtime/market_snapshot.schema.json
          index a83bd1c..0000000 100644
          --- a/schemas/runtime/market_snapshot.schema.json
          +++ b/schemas/runtime/market_snapshot.schema.json
          @@ -18,12 +18,28 @@
               "cash_rate",
               "quality_flags"
             ],
          +  "$defs": {
          +    "sourceObservation": {
          +      "type": "object",
          +      "$comment": "Runtime also enforces source_timestamp_utc <= available_at_utc <= decision_time_utc.",
          +      "required": ["source", "source_timestamp_utc", "available_at_utc", "payload_sha256", "quality"],
          +      "properties": {
          +        "source": {"type": "string", "minLength": 1},
          +        "source_timestamp_utc": {"type": "string", "pattern": "Z$"},
          +        "available_at_utc": {"type": "string", "pattern": "Z$"},
          +        "payload_sha256": {"type": "string", "pattern": "^sha256:[0-9a-f]{64}$"},
          +        "quality": {"enum": ["ok", "stale", "missing", "future", "invalid"]}
          +      },
          +      "additionalProperties": false
          +    }
          +  },
             "properties": {
               "schema_version": {"const": "1.0"},
               "snapshot_id": {"type": "string", "pattern": "^sha256:[0-9a-f]{64}$"},
               "as_of_utc": {"type": "string", "pattern": "Z$"},
               "decision_time_utc": {"type": "string", "pattern": "Z$"},
          -    "sources": {"type": "object", "minProperties": 1},
          +    "sources": {"type": "object", "minProperties": 1, "additionalProperties": {"$ref": "#/$defs/sourceObservation"}},
               "spot": {"type": "object"},
               "perp": {"type": "object"},
          diff --git a/tests/runtime/test_runtime_integrity.py b/tests/runtime/test_runtime_integrity.py
          index d7c7e3d..0000000 100644
          --- a/tests/runtime/test_runtime_integrity.py
          +++ b/tests/runtime/test_runtime_integrity.py
          @@ -6,7 +6,7 @@ from decimal import Decimal
          
           from finruntime.canonical import ContractError
          -from finruntime.data.availability import evaluate_availability, seal_sources
          +from finruntime.data.availability import seal_sources
          @@ -57,31 +57,16 @@ class AvailabilityIntegrityTests(unittest.TestCase):
                   )
          
          -    def test_future_source_timestamp_blocks_risk_increase(self) -> None:
          +    def test_future_source_timestamp_is_rejected_by_snapshot_contract(self) -> None:
                   observation = self.observation(
                       source_time="2026-07-27T00:06:00Z",
                       available_at="2026-07-27T00:04:00Z",
                   )
          -        snapshot = MarketSnapshot.create(
          -            as_of_utc="2026-07-27T00:00:00Z",
          -            decision_time_utc="2026-07-27T00:05:00Z",
          -            sources={"spot_daily": observation},
          -        )
          -
          -        decision = evaluate_availability(
          -            snapshot,
          -            critical_sources=("spot_daily",),
          -        )
          -
          -        self.assertFalse(decision.risk_increase_permitted)
          -        self.assertIn(
          -            "source_timestamp_after_available_at:spot_daily",
          -            decision.blocking_reasons,
          -        )
          -        self.assertIn(
          -            "source_timestamp_after_decision_time:spot_daily",
          -            decision.blocking_reasons,
          -        )
          +        with self.assertRaises(ContractError):
          +            MarketSnapshot.create(
          +                as_of_utc="2026-07-27T00:00:00Z",
          +                decision_time_utc="2026-07-27T00:05:00Z",
          +                sources={"spot_daily": observation},
          +            )
          @@ -105,7 +90,7 @@ class PaperExecutionIntegrityTests(unittest.TestCase):
                   }
          
          -    def test_partial_plan_cannot_be_executed_twice(self) -> None:
          +    def test_partial_plan_resumes_without_overfill(self) -> None:
          @@ -175,6 +160,10 @@ class PaperExecutionIntegrityTests(unittest.TestCase):
               )
               self.assertEqual(first.fill_events[0].status, "partial")
               self.assertEqual(first.fill_events[0].filled_quantity, "5")
          +        intent_id = plan.intents[0].intent_id
          +        self.assertEqual(
          +            first.account_state.active_plan_filled_quantities[intent_id], "5"
          +        )
          
               retry_quote = PaperQuote(
          @@ -187,14 +176,31 @@ class PaperExecutionIntegrityTests(unittest.TestCase):
                   mid="100",
                   available_quantity="1000",
               )
          -        with self.assertRaises(AccountingHalt):
          -            execute_paper_cycle(
          -                plan=plan,
          -                account_state=first.account_state,
          -                quotes=(retry_quote,),
          -                mark_prices=self.reference_prices(),
          -                policy=policy,
          -            )
          +        second = execute_paper_cycle(
          +            plan=plan,
          +            account_state=first.account_state,
          +            quotes=(retry_quote,),
          +            mark_prices=self.reference_prices(),
          +            policy=policy,
          +        )
          +        self.assertEqual(second.fill_events[0].status, "filled")
          +        self.assertEqual(second.fill_events[0].filled_quantity, "15")
          +        self.assertEqual(second.account_state.spot_positions["BTC/USDT"], "20")
          +        self.assertTrue(second.execution_complete)
          +
          +        repeated = execute_paper_cycle(
          +            plan=plan,
          +            account_state=second.account_state,
          +            quotes=(retry_quote,),
          +            mark_prices=self.reference_prices(),
          +            policy=policy,
          +        )
          +        self.assertEqual(repeated.fill_events, ())
          +        self.assertEqual(repeated.account_state.account_hash, second.account_state.account_hash)
          +
          +        old_quote = PaperQuote(**{**retry_quote.__dict__, "observed_at_utc": "2026-07-27T00:05:30Z"}) if hasattr(retry_quote, "__dict__") else PaperQuote(
          +            instrument=retry_quote.instrument, market_type=retry_quote.market_type,
          +            observed_at_utc="2026-07-27T00:05:30Z", source_observation_hash="sha256:" + "4" * 64,
          +            bid=retry_quote.bid, ask=retry_quote.ask, mid=retry_quote.mid, available_quantity=retry_quote.available_quantity,
          +        )
          +        with self.assertRaises(AccountingHalt):
          +            execute_paper_cycle(plan=plan, account_state=first.account_state, quotes=(old_quote,), mark_prices=self.reference_prices(), policy=policy)
          diff --git a/tests/runtime/test_paper_ledger.py b/tests/runtime/test_paper_ledger.py
          index c34f738..0000000 100644
          --- a/tests/runtime/test_paper_ledger.py
          +++ b/tests/runtime/test_paper_ledger.py
          @@ -292,6 +292,17 @@ class PaperLedgerTests(unittest.TestCase):
                   )
                   self.assertEqual([fill.status for fill in result.fill_events], ["partial", "rejected"])
                   self.assertEqual(result.outcomes[1].reason, "parent_intent_not_fully_filled")
                   self.assertEqual(result.account_state.perp_positions["BTC/USDT:USDT"], "1")
          +        resumed = execute_paper_cycle(
          +            plan=plan,
          +            account_state=result.account_state,
          +            quotes=[self.quote(market_type="perpetual", available="100", time="2026-07-27T00:07:00Z")],
          +            mark_prices=self.prices(),
          +            policy=policy,
          +        )
          +        self.assertEqual(
          +            [fill.status for fill in resumed.fill_events], ["filled", "filled"]
          +        )
          +        self.assertEqual(resumed.account_state.perp_positions["BTC/USDT:USDT"], "-3")
          +        self.assertTrue(resumed.execution_complete)
          diff --git a/tests/runtime/test_journal.py b/tests/runtime/test_journal.py
          index 178f2c2..0000000 100644
          --- a/tests/runtime/test_journal.py
          +++ b/tests/runtime/test_journal.py
          @@ -71,6 +71,13 @@ class JournalTests(unittest.TestCase):
               def test_conflicting_singleton_event_is_rejected(self) -> None:
                   with tempfile.TemporaryDirectory() as directory:
                       journal = AppendOnlyJournal(Path(directory) / "events.jsonl")
          +            journal.append(
          +                event_type="SNAPSHOT_ACCEPTED",
          +                event_time_utc="2026-07-27T00:04:59Z",
          +                strategy_id="v75_atlas_nx",
          +                sequence=7,
          +                payload={"snapshot": "a"},
          +            )
                       journal.append(
                           event_type="TARGET_COMPUTED",
                           event_time_utc="2026-07-27T00:05:00Z",
          @@ -88,6 +95,59 @@ class JournalTests(unittest.TestCase):
                               payload={"target": "b"},
                           )
          
          +    def test_semantic_regressions_fail_before_append(self) -> None:
          +        with tempfile.TemporaryDirectory() as directory:
          +            journal = AppendOnlyJournal(Path(directory) / "events.jsonl")
          +            journal.append(
          +                event_type="SNAPSHOT_ACCEPTED",
          +                event_time_utc="2026-07-27T00:05:00Z",
          +                strategy_id="v75_atlas_nx",
          +                sequence=2,
          +                payload={"snapshot": 2},
          +            )
          +            with self.assertRaises(JournalCorruptionError):
          +                journal.append(
          +                    event_type="SNAPSHOT_ACCEPTED",
          +                    event_time_utc="2026-07-27T00:06:00Z",
          +                    strategy_id="v75_atlas_nx",
          +                    sequence=1,
          +                    payload={"snapshot": 1},
          +                )
          +            self.assertEqual(len(journal.verify()), 1)
          +
          +            with self.assertRaises(JournalCorruptionError):
          +                journal.append(
          +                    event_type="TARGET_COMPUTED",
          +                    event_time_utc="2026-07-27T00:04:00Z",
          +                    strategy_id="v75_atlas_nx",
          +                    sequence=2,
          +                    payload={"target": 2},
          +                )
          +            self.assertEqual(len(journal.verify()), 1)
          +
          +    def test_runtime_phase_requires_predecessors(self) -> None:
          +        with tempfile.TemporaryDirectory() as directory:
          +            journal = AppendOnlyJournal(Path(directory) / "events.jsonl")
          +            journal.append(
          +                event_type="SNAPSHOT_ACCEPTED",
          +                event_time_utc="2026-07-27T00:05:00Z",
          +                strategy_id="v75_atlas_nx",
          +                sequence=1,
          +                payload={"snapshot": 1},
          +            )
          +            with self.assertRaises(JournalCorruptionError):
          +                journal.append(
          +                    event_type="PLAN_CREATED",
          +                    event_time_utc="2026-07-27T00:05:01Z",
          +                    strategy_id="v75_atlas_nx",
          +                    sequence=1,
          +                    payload={"plan": 1},
          +                )
          +            self.assertEqual(len(journal.verify()), 1)
          +
               def test_corruption_fails_closed(self) -> None:
          PATCH
          git apply --check /tmp/runtime-causality.patch
          git apply /tmp/runtime-causality.patch
          PYTHONPATH=src python -m compileall -q src/finruntime tests/runtime
          PYTHONPATH=src python -m unittest discover -s tests/runtime -p 'test_*.py' -v
          python scripts/verify_runtime.py --contracts-only
          rm .github/workflows/agent-runtime-causality-patch.yml
          git config user.name 'OpenAI Agent'
          git config user.email 'agent@openai.com'
          git add -A
          git commit -m 'Add resumable paper progress and journal semantics'
          git push origin HEAD:agent/runtime-causality-progress-20260730
