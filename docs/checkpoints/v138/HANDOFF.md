# V138 handoff

Start with `CHECKPOINT_V138.json`, `RESEARCH_CHECKPOINT_V138_RU.md`, and `ANNUAL_RETURNS.csv`.

The complete reproducible dependency tree is stored under `research/active_v131_v138/dependencies/atlas`. The exact frozen V75 control is included and every candidate comparison contains its yearly returns.

No candidate in V131–V138 is promoted. V136 is a near-miss execution layer only. Do not relax the frozen gates after reading the results.
