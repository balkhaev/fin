# V405–V412 — champion refinement

Широкий исторический поиск завершён. Этот цикл **не создаёт новую alpha-grid** и не меняет frozen-параметры.

## Роли

- `V75_ATLAS_NX` — primary paper/shadow champion;
- `V136_execution_plateau` — exact execution-shadow;
- `V28_growth_control` — обязательный независимый control;
- V285 и V365 — rejected-after-OOS anti-controls, не получают капитал.

## Цели

1. Зафиксировать source/provenance V75 и V136.
2. Измерить реальную величину исторического execution-uplift V136 относительно V75.
3. Заморозить paper-forward acceptance gates до новых наблюдений.
4. Перейти от поиска новых сигналов к reconciliation, slippage, turnover и drawdown discipline.

```text
historical_parameter_search = closed
live_ready = false
real_leverage_authorized = false
integration_permitted = false
```
