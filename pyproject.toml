[build-system]
requires = ["setuptools>=61", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "finruntime"
version = "0.3.0"
description = "Deterministic FIN paper/shadow runtime and read-only control room"
requires-python = ">=3.11"
dependencies = []

[project.scripts]
finruntime = "finruntime.cli:main"
fin-control-room = "finruntime.observability.server:main"
fin-paper-scheduler = "finruntime.scheduler.cli:main"

[project.optional-dependencies]
runtime-dev = []

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
"finruntime.observability" = ["backtest_data/*.json"]
